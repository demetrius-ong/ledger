# Ledger source

`main.js` is the readable source for `../app.js` (which is the bundled, minified build the app actually runs).

To rebuild after editing `main.js`:

    cd source
    npm install
    npm run build

Page layout and styles live in `../index.html`. Firebase settings live in `../config.js`.
